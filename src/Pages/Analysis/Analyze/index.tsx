import React, { ReactElement } from 'react';
import styled from 'styled-components/macro';

const AnalyzePage = styled.div``;

const Analyze = (): ReactElement => <AnalyzePage></AnalyzePage>;

export default Analyze;
