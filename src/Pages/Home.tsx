import React, { ReactElement } from 'react';
import P from 'Atoms/P';

const Home = (): ReactElement => (
  <P fontSize="medium" fontWeight="light">
    Use the links to navigate landing page.
  </P>
);

export default Home;
