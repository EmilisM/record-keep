import React, { ReactElement } from 'react';
import P from 'Atoms/Text/P';

const LandingNotFound = (): ReactElement => (
  <P fontSize="medium" fontWeight="light" color="primaryLight">
    404
  </P>
);

export default LandingNotFound;
