import { RouteConfigType } from 'Types/Route';

const RouteConfig: RouteConfigType = {
  Root: '/',
  Home: '/home',
  Login: '/login',
  Dashboard: '/dashboard',
  About: '/about',
};

export default RouteConfig;
