import { DefaultTheme } from 'styled-components/macro';

const theme: DefaultTheme = {
  colors: {
    background: {
      primary: '#41295A',
      secondary: '#2F0743',
    },
  },
};

export default theme;
