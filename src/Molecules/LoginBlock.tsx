import React, { ReactElement } from 'react';

import LoginSwitcher from 'Molecules/LoginSwitcher';

const LoginBlock = (): ReactElement => {
  return <LoginSwitcher />;
};

export default LoginBlock;
